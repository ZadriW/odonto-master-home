query ShippingQuotesCheckout($checkoutId: Uuid!, $useSelectedAddress: Boolean) {
  shippingQuotes(
    checkoutId: $checkoutId
    useSelectedAddress: $useSelectedAddress
  ) {
    name
    shippingQuoteId
    type
    value
  }
}

